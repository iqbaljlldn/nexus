name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  GO_VERSION: "1.25.x"
  NODE_VERSION: "24.11.1"
  PNPM_VERSION: "10.24.0"

jobs:
  format-check:
    name: Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Check Go Format
        run: |
          if [ -n "$(gofmt -l .)" ]; then
            echo "The following Go files are not formatted:"
            gofmt -l .
            exit 1
          fi

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check Frontend Format (Biome)
        run: cd apps/web && pnpm exec biome check .

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest
          args: ./apps/api/... ./pkg/...

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint Frontend (Biome)
        run: cd apps/web && pnpm exec biome lint .

  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Run gosec (api)
        uses: securego/gosec@master
        with:
          args: ./apps/api/...

      - name: Run gosec (pkg)
        uses: securego/gosec@master
        with:
          args: ./pkg/...

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest

      - name: Run govulncheck (api)
        working-directory: apps/api
        run: govulncheck ./...

      - name: Run govulncheck (pkg)
        working-directory: pkg
        run: govulncheck ./...

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Run Go Tests (api)
        working-directory: apps/api
        run: go test ./... -race -cover

      - name: Run Go Tests (pkg)
        working-directory: pkg
        run: go test ./... -race -cover

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [format-check, lint, security-scan, test]
    steps:
      - uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Build Go binaries (api)
        working-directory: apps/api
        run: go build ./...

      - name: Build Go binaries (pkg)
        working-directory: pkg
        run: go build ./...
