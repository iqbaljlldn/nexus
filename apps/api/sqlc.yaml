version: "2"
sql:
  - schema: "../../migrations"
    queries: "internal/identity/infrastructure/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "infrastructure"
        out: "internal/identity/infrastructure"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  - schema: "../../migrations"
    queries: "internal/workspace/infrastructure/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "infrastructure"
        out: "internal/workspace/infrastructure"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  - schema: "../../migrations"
    queries: "internal/member/infrastructure/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "infrastructure"
        out: "internal/member/infrastructure"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  - schema: "../../migrations"
    queries: "internal/role/infrastructure/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "infrastructure"
        out: "internal/role/infrastructure"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  - schema: "../../migrations"
    queries: "internal/channel/infrastructure/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "infrastructure"
        out: "internal/channel/infrastructure"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false
